# Test Environment Variables
NODE_ENV=test

# Test Database (use separate DB for tests)
DATABASE_URL=postgresql://devflow:changeme@localhost:5432/devflow_test

# Temporal (can use same instance)
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=test
TEMPORAL_TASK_QUEUE=devflow-test

# Optional: Real credentials for integration tests
# Leave empty to skip integration tests
GITHUB_TOKEN=
NOTION_API_KEY=
NOTION_DATABASE_ID=
ANTHROPIC_API_KEY=

# Test settings
JEST_SILENT=false

