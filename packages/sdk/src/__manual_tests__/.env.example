# DevFlow Integration Tests - Environment Variables

# Required - Database connection
DATABASE_URL=postgresql://devflow:changeme@localhost:5432/devflow?schema=public

# Required - OAuth encryption key (32 bytes base64)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
OAUTH_ENCRYPTION_KEY=your-base64-encryption-key

# Required - Project to test
PROJECT_ID=your-project-id

# Optional - Redis configuration (defaults shown)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL=redis://localhost:6379

# Optional - GitHub test configuration
GITHUB_OWNER=facebook
GITHUB_REPO=react
GITHUB_ISSUE=1

# Optional - Linear test configuration
LINEAR_ISSUE_ID=DEV-1
LINEAR_STATUS=To Refinement

# Optional - Figma test configuration
FIGMA_FILE_KEY=TfJw2zsGB11mbievCt5c3n
FIGMA_NODE_ID=12252-33902

# Optional - Sentry test configuration (if not set, Sentry tests will be skipped)
# SENTRY_ISSUE_ID=1234567890
